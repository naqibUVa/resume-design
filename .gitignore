node_modules/
dist/
dist-ssr/
*.local

# Editors
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Your own résumé data, if you keep an exported copy in the repo.
# Delete this line if you *want* to version your CV alongside the code.
my-resume-*.json

# The live document in data/ is committed by default, so your CV travels with
# the project and every draft is recoverable. Uncomment this before pushing to a
# PUBLIC repository — the file holds your email, phone number and city.
# data/resume.json

# Rendered theme screenshots from `npm run shots` — regenerate, do not commit.
.shots/
